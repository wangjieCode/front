# 2026-02-10

## 背景

- 修复 monorepo 架构评审中发现的问题：无效脚本、构建语义错误、Turbo 配置不规范、缓存产物声明错误、多锁文件并存。

## 变更

- `backend/package.json`
  - `build` 改为 `node --import tsx scripts/build.ts`（运行时模式）。
  - `start` 改为 `node --import tsx src/index.ts`。
  - `start:worker` 改为 `node --import tsx src/worker.ts`。
  - `tsx` 调整为运行时依赖，支持生产环境 `--prod` 安装后直接启动。
  - 删除已不存在脚本文件对应的 scripts 项。
  - 补充现存脚本入口：`test:path-utils`、`test:new-prompt`。
- `backend/scripts/build.ts`
  - 新增 tsx 构建入口脚本，用于 monorepo 构建链路统一。
- `turbo.json`
  - 将 `pipeline` 更正为 `tasks`。
  - `build.outputs` 调整为 `dist/**`、`build/**`、`backend/public/**`。
  - `dev` 任务补充 `persistent: true`。
- `package.json`（根）
  - 增加 `packageManager: pnpm@10.14.0`，修复 Turbo 2.x 的 workspace 解析失败。
- `packages/shared/package.json`
  - `types` 从 `./src/index.ts` 调整为 `./dist/index.d.ts`。
  - `exports["."].types` 置于 `import/require` 之前，消除 `types condition will never be used` 告警。
- `frontend/package.json`
  - 增加 `"type": "module"`，消除 Vite CJS Node API 弃用告警。
- `deploy.mjs`
  - 部署前构建改为工作区级 `pnpm install --frozen-lockfile` + `pnpm turbo run build`（带 filter）。
  - 打包内容改为 monorepo 运行必需集：根 `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml`、`backend/src`、`backend/public`、`packages/shared/dist` 等。
  - 远端安装改为在工作区根执行 `pnpm install --prod --frozen-lockfile --filter @front/shared --filter web-frontend-intern-assistant-backend`，兼容 `workspace:*` 与 `tsx` 运行依赖。
- 锁文件治理
  - 删除 `backend/pnpm-lock.yaml`、`frontend/pnpm-lock.yaml`。
  - 删除根目录 `package-lock.json`，统一 pnpm。
  - `.gitignore` 增加对子包 `pnpm-lock.yaml` 的忽略。
- 文档
  - 新增规格：`docs/specs/spec-monorepo-workspace.md`。
  - 更新规格索引与技术架构说明。
- 本地冲突处理
  - 解决 `frontend/src/components/ConversationView.tsx` 合并冲突，统一消息发送逻辑为 `options?.modelOverride` + `images` 透传 + 模型合法性校验。
- 创建对话图片支持
  - 桌面端与移动端“创建对话”入口新增图片附件上传与预览。
  - 创建成功后通过路由状态透传 `initialImages`，首条 `autoSend` 消息与文本一并发送图片附件。

## 影响

- 根 `turbo build/test` 语义恢复为“执行并结束”的任务，不再被后端启动逻辑阻塞。
- 依赖锁定策略统一到根锁文件，减少工作区依赖解析偏差。

## 验证

- 已执行：
  - `pnpm install`：成功（用户本地执行）。
  - `pnpm -w build`：Turbo 工作区已可解析；失败点为 backend 现存 TypeScript 类型错误（与本次 monorepo 架构修复无直接耦合）。
  - `pnpm -w test`：Turbo 工作区已可解析；失败点为 frontend Jest 环境缺少 `TextEncoder`（测试环境配置问题）。
  - `pnpm --dir packages/shared dev`：已无 `types condition` 警告。
  - `pnpm --dir frontend build`：已无 Vite CJS Node API 弃用警告，构建成功。
  - `node --check deploy.mjs`：语法检查通过。

## 风险

- `pnpm-lock.yaml` 需要在一次完整安装后刷新，避免历史锁文件残留导致 CI 偏差。

## 本次追加（会话元数据 images 列）

### 背景

- 后端在查询 `message_metadata` 时包含 `images` 字段，但数据库缺少该列，导致 `column "images" does not exist`。

### 变更

- 新增 Drizzle 迁移：`backend/drizzle/0004_add_message_metadata_images.sql`。
- 更新迁移索引：`backend/drizzle/meta/_journal.json` 增加 `0004_add_message_metadata_images`。
- 新增快照：`backend/drizzle/meta/0004_snapshot.json`，补齐 `message_metadata.images` 字段。
- 删除未接入主迁移链的重复 SQL：`backend/src/db/migrations/add_images_to_message_metadata.sql`。
- 文档同步：
  - `docs/specs/spec-conversations.md` 增加 `message_metadata.images` 数据约束。
  - `docs/technical/data_model.md` 补充 `message_metadata.images` 字段。

### 验证

- 迁移 SQL 与 Drizzle 元数据已对齐，可通过 `pnpm --dir backend db:migrate` 落库。

## 本次追加（图片两阶段处理）

### 背景

- 图片消息此前走“直接视觉模型返回”分支，未继续进入现有主 AI（Neovate）执行链路。

### 变更

- `backend/src/services/ConversationAIService.ts`
  - 新增图片两阶段处理：
    - 第一步：调用视觉模型提炼图片关键信息。
    - 第二步：将“用户原始需求 + 图片关键信息”汇总成增强提示词，继续调用现有 Neovate `modifyCode/modifyCodeStream`。
  - 移除“有图时直接返回视觉模型结果并提前结束”的旧分支。
  - 当检测到图片但未配置视觉模型时，直接返回配置错误，避免静默忽略图片。
  - 视觉模型请求增加 system 指令，约束输出为图片关键信息摘要。

### 验证

- 已执行：`pnpm --dir backend exec tsc --noEmit`（当前仓库存在其他历史类型错误；本次改动文件 `ConversationAIService.ts` 未新增类型错误）。

## 本次追加（视觉模型单测）

### 变更

- 新增并扩展测试：`backend/src/__tests__/conversationAIService.test.ts`
  - 覆盖无图场景下的主 AI 调用参数断言。
  - 覆盖有图场景下“先调用视觉模型，再将提炼信息汇总后发送给主 AI”的链路断言：
    - 断言会调用视觉模型接口 `chat/completions`。
    - 断言请求中包含 `image_url` 内容。
    - 断言传给主 AI 的 prompt 包含“用户原始需求 + 图片关键信息”。

### 验证

- 已执行：`pnpm --dir backend exec jest src/__tests__/conversationAIService.test.ts --runInBand`
- 结果：2/2 用例通过。

## 本次追加（视觉提供方独立校验脚本）

### 背景

- 需要独立验证视觉模型提供方是否可用，快速校验 `MIDSCENE_MODEL_BASE_URL` 与 `MIDSCENE_MODEL_API_KEY`。

### 变更

- 新增脚本：`backend/scripts/verify-vision-model.mjs`
  - 校验关键环境变量是否存在。
  - 对 `chat/completions` 发送最小视觉请求（包含 `image_url`）。
  - 支持参数覆盖：`--model`、`--base-url`、`--api-key`、`--prompt`、`--timeout`、`--image-url`。
  - 成功返回可读摘要；失败返回非 0 并输出明确错误原因。
- 新增 npm 脚本：`backend/package.json` -> `verify:vision`
  - 命令：`pnpm --dir backend verify:vision`
- 新增规格文档：`docs/specs/spec-vision-model-verify.md`
- 更新规格索引：`docs/specs/README.md`
- 更新示例环境变量：`backend/.env.example` 增加 MIDSCENE 视觉模型配置项。

### 验证

- 可通过 `pnpm --dir backend verify:vision` 独立执行视觉提供方可用性校验。

## 本次追加（默认视觉模型切换）

### 变更

- 视觉模型默认值统一改为 `qwen3-vl-plus`：
  - `backend/src/services/ConversationAIService.ts` 视觉分支默认模型由通用编码模型兜底改为 `qwen3-vl-plus`。
  - `backend/scripts/verify-vision-model.mjs` 默认模型与 help 文案改为 `qwen3-vl-plus`。
  - `backend/.env.example` 的 `MIDSCENE_MODEL_NAME` 示例默认值改为 `qwen3-vl-plus`。
- 规格同步：
  - `docs/specs/spec-vision-model-verify.md` 增加默认模型规则与迭代记录。

### 验证

- `pnpm --dir backend verify:vision -- --help` 可看到默认模型文案为 `qwen3-vl-plus`。

## 本次追加（视觉校验默认图片尺寸修复）

### 背景

- `verify:vision` 默认内置测试图为 1x1，`qwen3-vl-plus` 返回参数错误：图片宽高至少需大于 10。

### 变更

- `backend/scripts/verify-vision-model.mjs`
  - 默认测试图从 1x1 PNG 调整为 32x32 PNG，避免触发模型最小分辨率限制。
- 文档同步：
  - `docs/specs/spec-vision-model-verify.md` 补充默认测试图尺寸规则。

### 验证

- 使用默认参数执行 `pnpm --dir backend verify:vision` 时，不再因默认图片尺寸触发 `InvalidParameter`（需在有可用网络与有效 key 的环境验证）。

## 本次追加（视觉特征日志输出）

### 变更

- `backend/src/services/ConversationAIService.ts`
  - 图片两阶段流程中，新增日志打印“视觉模型提炼特征”。
- `backend/scripts/verify-vision-model.mjs`
  - 校验成功后新增“视觉模型提取特征”输出，打印完整特征文本。
- 规格同步：
  - `docs/specs/spec-conversations.md` 增加视觉特征日志要求。
  - `docs/specs/spec-vision-model-verify.md` 增加成功输出特征内容要求。

### 验证

- `pnpm --dir backend verify:vision` 成功时将输出“视觉模型提取特征”段落。

## 本次追加（视觉模型配置落地）

### 背景

- 需要先完成本地运行配置，确保视觉模型默认值明确，不依赖隐式兜底。

### 变更

- 环境变量补齐：
  - `backend/.env` 新增 `MIDSCENE_MODEL_NAME=qwen3-vl-plus`。
  - `backend/.env.production` 新增：
    - `MIDSCENE_MODEL_BASE_URL=https://apis.iflow.cn/v1`
    - `MIDSCENE_MODEL_API_KEY=<已配置>`
    - `MIDSCENE_MODEL_NAME=qwen3-vl-plus`
- 全局 Neovate 配置（运行环境）：
  - `~/.neovate/config.json` 设置：
    - `model=iflow/qwen3-coder-plus`
    - `visionModel=iflow/qwen3-vl-plus`

### 验证

- 已执行：`pnpm --dir backend verify:vision -- --help`，确认视觉校验脚本参数入口可用。
