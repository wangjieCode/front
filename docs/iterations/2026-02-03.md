# 迭代记录：2026-02-03

## 背景

- 处理前端入口文件合并冲突，统一应用逻辑来源。
- 补充移动端入口分流能力，按 User-Agent 选择桌面或移动端入口。

## 变更

- 解决 `frontend/src/App.tsx` 冲突，保留基于 `useAppLogic` 的路由与状态管理实现。
- 移除冲突分支中重复的内联 `AppContent`/`ChatRoute` 逻辑片段。
- 移动端入口路由基准统一为 `/m`，避免地址前缀导致的路由失配。
- 后端新增 `/m` 分流逻辑，移动端请求重定向到移动入口，桌面端请求回退至桌面入口。
- 新增移动端入口分流规格说明。
- 本地开发服务为 `/m` 与子路径提供 `mobile.html`，确保开发与生产路由一致。
- 调整 Vite 中间件顺序，优先匹配 `/m` 并返回移动端入口。
- 新增移动端创建对话组件，移动端首页使用该组件完成会话创建。
- 创建对话回调支持传递模型参数，确保移动端与桌面端选择生效。
- 优化对话列表布局与视觉层级，统一操作区与状态标识样式。
- 移动端与桌面端 UI 组件拆分，移动端改为使用独立页面与组件实现。

## 影响

- 应用入口逻辑保持一致，避免重复状态与路由实现并存。
- 移动端访问路径统一为 `/m`，桌面与移动入口可按 User-Agent 自动分流。

## 验证

- 未执行（按要求）。

## 风险

- 无新增风险。
