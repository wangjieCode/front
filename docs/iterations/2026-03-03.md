# 迭代记录：2026-03-03

## 背景

- 需要将 Zadig 手工发布流程抽象为可复用 skill，实现“在 x 环境发布 x 项目的 x 分支”。
- 现有链路依赖页面操作，分支切换与任务触发不易标准化。

## 变更

- 新增 Skill：`skills/zadig-workflow-deploy/`
  - 新增：`skills/zadig-workflow-deploy/SKILL.md`
  - 新增：`skills/zadig-workflow-deploy/scripts/deploy_workflow.mjs`
  - 新增：`skills/zadig-workflow-deploy/references/zadig-api-chain.md`
  - 新增：`skills/zadig-workflow-deploy/agents/openai.yaml`
- 新增规格文档：`docs/specs/spec-zadig-workflow-deploy-skill.md`
- 更新规格索引：`docs/specs/README.md` 增加新规格入口。
- 修复发布范围控制：
  - 更新：`skills/zadig-workflow-deploy/scripts/deploy_workflow.mjs`
  - 默认仅触发命中 repo 的 `targets`，避免同分组全部服务被触发。
  - 新增强校验：最终 payload 必须仅包含 1 个 target，否则失败退出。
  - 参数收敛：仅保留 `--workflow`、`--repo`、`--branch`。
  - 固定配置：`base-url=https://zadig.dtminds.cn`、`project=smp`，并从 `ZADIG_TOKEN` 读取 token。
  - workflow 白名单：仅允许 `FE-test01`、`test02`、`test03`，env 自动推导。
  - 更新：`skills/zadig-workflow-deploy/SKILL.md` 使用说明。
  - 更新：`docs/specs/spec-zadig-workflow-deploy-skill.md` 业务规则与功能需求。

## 影响

- 发布动作可通过参数化命令触发，减少 UI 手工步骤。
- 统一基于 preset payload 修改分支，避免重复组装请求体。
- 不再保留任何明文账号或 token 到仓库文件。

## 验证

- 执行 `node skills/zadig-workflow-deploy/scripts/deploy_workflow.mjs --help`，确认参数说明可用。
- 执行 `node skills/zadig-workflow-deploy/scripts/deploy_workflow.mjs --help`，确认仅保留单目标触发参数说明。
- 执行 `python3 /Users/gangqiang/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/zadig-workflow-deploy`，确认 skill 结构合法。

## 风险

- 未在当前环境对真实 Zadig API 发起联调（网络与凭证限制），首次实跑需用有效 token 验证接口可用性。
- 若 Zadig API 字段调整，脚本中的 preset 字段映射需同步更新。
