# 2026-01-30 迭代记录

## 背景

- 文档结构与内容需要全量重建，覆盖业务与技术。

## 变更

- 删除旧 docs 并重新建立目录结构。
- 新增 context / specs / business / technical / iterations / templates。
- 全量重写文档内容。
- 统一 Worktree 路径规则为 `{WORKTREE_BASE_DIR}/project-{projectId}/user-{userId}/conversation-{sessionId}`，以代码实现为准。
- 移除环境变量 `GITLAB_PROJECT_ID` 描述，与实现一致。
- 基线分支规格不再声明“创建对话时推送分支”，与实现一致。
- 创建对话选择项目后不再展示仓库与工作目录。
- 基线分支列表改为分页拉取，并在下拉展开时刷新以显示最新分支。
- AI 自动提交后推送改为以当前 worktree 分支为准，避免推送到基线分支。
- 自动推送失败时尝试设置 upstream，确保新分支可推送到远端。
- 新增基线分支选择：按项目 GitLab 分支列表选择基线分支。
- 创建对话时传 `baseBranch`，后端使用 projects.gitlab_project_id 请求 GitLab。
- 移除 `GITLAB_PROJECT_ID` 依赖。
- 增加后端启动排查日志（数据库/服务初始化与监听阶段）。
- 增加数据库连接串脱敏日志输出。
- 新增模型选择（默认 `Qwen3-Coder-Plus`），创建对话时写入上下文并传递给 Neovate CLI。
- 调整新建对话页布局：模型选择移动到发送按钮左侧。
- 新建对话页顶部表单收敛为单行，模型选择弱化为次要控件。
- 修正新建对话页顶部表单对齐问题。
- 调整对话模式选择器高度，与左侧选择框一致。
- 模型参数改为 iflow provider 格式（例如 `iflow/qwen3-coder-plus`）。
- 对话页模型展示移动到输入区，不占用头部区域。
- 对话页底部输入区布局参考 Gemini：顶部工具行 + 输入区一体化卡片。
- 对话页模型选择移动到发送按钮旁，发送消息时附带 model 参数（不持久化）。
- 修复 MessageInput actions 属性未解构导致的运行时错误。
- 修复自动发送首条消息未使用新建时模型参数的问题。
- 发送消息时仅在模型合法时透传，非法值忽略避免 Neovate 报错。
- 修复创建对话时重复声明 model 变量导致的后端启动失败。
- 修复对话消息流式处理中 model 变量未定义导致的异常。

## 影响

- 所有文档路径与引用需要重新适配。

## 验证

- 目录结构符合 README 指引。
- 规格文档覆盖核心功能。

## 风险

- 外部引用旧路径会失效。
