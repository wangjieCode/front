# 迭代记录：2026-02-02

## 背景

- 需要快速验证 IFLOW_API_KEY 是否可用，并能与 Neovate 模型完成一次最小交互。

## 变更

- 新增 Neovate SDK 验证脚本，发送最小提示词并检查响应。
- 后端新增 `verify:neovate` 脚本入口（Node ESM 脚本执行）。
- 验证日志输出脱敏后的 IFLOW_API_KEY（保留前后 4 位）。
- 验证脚本优先加载 `.env.production`，不存在时加载 `.env`。
- 部署流程在安装依赖后新增 API Key 验证步骤。
- 补充 Neovate SDK 验证脚本规格文档。

## 影响

- 可通过命令行快速确认 API Key 与模型交互是否通畅。

## 验证

- 未执行（按要求）。

## 风险

- SDK 返回结构变化可能影响脚本判断逻辑，需要随版本调整。
