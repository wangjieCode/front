# 迭代记录：2026-03-09

## 背景

- 前端桌面与移动入口仍使用 Vite 默认标签页图标 `vite.svg`，与“代码伙计 Code Mate”品牌不一致。

## 变更

- 新增品牌 favicon 资源：`frontend/src/favicon.svg`（`CM` 字母图标）。
- 更新桌面入口 `frontend/index.html`，将标签页图标引用改为 `/src/favicon.svg`。
- 更新移动入口 `frontend/mobile.html`，将标签页图标引用改为 `/src/favicon.svg`。
- 更新规格文档 `docs/specs/spec-mobile-entry.md`，补充入口统一 favicon 约束与本次迭代记录。

## 影响

- 浏览器标签页图标由默认 Vite 图标统一为项目品牌图标，桌面与移动入口表现一致。
- 移除旧的默认图标引用，不保留兼容分支，入口逻辑更简洁。

## 验证

- 执行 `pnpm -C frontend build`，通过。

## 风险

- 当前 favicon 为纯 SVG 资源；若后续需要兼容极老浏览器，可再补充 `.ico`，本次按现有项目标准不做兼容扩展。
