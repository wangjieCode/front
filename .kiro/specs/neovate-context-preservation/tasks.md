# 实现计划

- [x] 1. 创建 Neovate 会话管理基础设施
  - 创建 NeovateSessionInfo 接口和相关类型定义
  - 实现文件系统存储结构
  - _需求: 2.1, 3.3_

- [ ] 2. 实现 NeovateSessionManager 核心功能
  - [x] 2.1 实现会话 ID 的保存和获取方法
    - 编写 saveSessionId 方法，保存任务 ID 到 Neovate 会话 ID 的映射
    - 编写 getSessionId 方法，根据任务 ID 获取 Neovate 会话 ID
    - 实现文件系统持久化逻辑
    - _需求: 1.2, 2.1, 3.3_

  - [ ]* 2.2 编写属性测试：会话映射持久化
    - **Property 7: 会话映射持久化**
    - **验证: 需求 2.3**

  - [x] 2.3 实现会话信息查询和删除
    - 编写 getSessionInfo 方法，返回完整的会话信息
    - 编写 deleteSession 方法，删除会话映射
    - _需求: 3.1, 3.2_

  - [ ]* 2.4 编写属性测试：会话信息完整性
    - **Property 9: 会话信息完整性**
    - **验证: 需求 3.3**

  - [x] 2.5 实现过期会话清理功能
    - 编写 cleanupExpiredSessions 方法
    - 实现 24 小时过期逻辑
    - _需求: 5.4_

  - [ ]* 2.6 编写属性测试：过期会话清理
    - **Property 11: 过期会话清理**
    - **验证: 需求 5.4**

- [ ] 3. 实现会话 ID 提取逻辑
  - [x] 3.1 编写会话 ID 提取函数
    - 实现 extractSessionId 函数，从 Neovate 输出中提取会话 ID
    - 支持多种可能的会话 ID 格式（UUID、短 ID、时间戳等）
    - 处理提取失败的情况
    - _需求: 1.2_

  - [ ]* 3.2 编写属性测试：会话 ID 提取和保存
    - **Property 2: 会话 ID 提取和保存**
    - **验证: 需求 1.2**

  - [ ]* 3.3 编写单元测试：会话 ID 提取
    - 测试各种格式的会话 ID 提取
    - 测试无效输出的处理
    - 测试边缘情况（空输出、超长输出等）

- [ ] 4. 更新 NeovateProvider 以支持会话管理
  - [x] 4.1 修改 buildCommand 方法支持 --resume 参数
    - 添加可选的 sessionId 参数
    - 当 sessionId 存在时，添加 `--resume <session-id>` 到命令中
    - _需求: 1.1, 1.3_

  - [ ]* 4.2 编写属性测试：首次调用不使用会话恢复
    - **Property 1: 首次调用不使用会话恢复**
    - **验证: 需求 1.1**

  - [ ]* 4.3 编写属性测试：后续调用使用会话恢复
    - **Property 3: 后续调用使用会话恢复**
    - **验证: 需求 1.3**

  - [ ] 4.4 更新 modifyCodeStream 方法集成会话管理
    - 添加 taskId 参数
    - 在执行前查询是否存在会话 ID
    - 在执行后提取并保存新的会话 ID
    - 处理会话恢复失败的情况
    - _需求: 1.1, 1.2, 1.3, 1.6_

  - [ ]* 4.5 编写属性测试：会话恢复失败处理
    - **Property 6: 会话恢复失败处理**
    - **验证: 需求 1.6**

  - [ ] 4.6 实现工作目录切换检测
    - 比较当前工作目录与保存的工作目录
    - 如果不同，删除旧会话并创建新会话
    - _需求: 1.5_

  - [ ]* 4.7 编写属性测试：工作目录切换创建新会话
    - **Property 5: 工作目录切换创建新会话**
    - **验证: 需求 1.5**

- [ ] 5. 更新 TaskOrchestrator 集成会话管理
  - [ ] 5.1 在任务执行时传递 taskId 给 NeovateProvider
    - 修改 step2_ModifyCode 方法
    - 修改 step3_QueryCode 方法
    - 确保 taskId 正确传递
    - _需求: 1.4, 2.1_

  - [ ]* 5.2 编写属性测试：新任务创建新映射
    - **Property 4: 新任务创建新映射**
    - **验证: 需求 1.4**

  - [ ] 5.3 在任务完成时保留会话记录
    - 确保任务完成或失败时不删除会话映射
    - 添加日志记录会话保留
    - _需求: 2.2_

- [ ] 6. 实现并发安全和性能优化
  - [ ] 6.1 添加并发控制机制
    - 在 NeovateSessionManager 中添加锁机制
    - 确保多个任务并发执行时会话映射不冲突
    - _需求: 2.4_

  - [ ]* 6.2 编写属性测试：并发任务隔离
    - **Property 8: 并发任务隔离**
    - **验证: 需求 2.4**

  - [ ] 6.3 实现内存缓存
    - 添加会话映射的内存缓存
    - 实现缓存失效策略
    - 减少文件 I/O 操作

- [ ] 7. 验证 API 密钥传递
  - [ ] 7.1 确认 LocalExecutor 和 SSHExecutor 正确传递 IFLOW_API_KEY
    - 检查现有实现
    - 添加日志记录 API 密钥是否存在（不记录实际值）
    - _需求: 4.1, 4.2_

  - [ ]* 7.2 编写属性测试：API 密钥传递
    - **Property 10: API 密钥传递**
    - **验证: 需求 4.1, 4.2**

  - [ ] 7.3 改进 API 密钥错误处理
    - 检测 Neovate 因 API 密钥问题失败的情况
    - 返回明确的错误信息
    - _需求: 4.3, 4.4_

- [ ] 8. 添加会话管理 API 端点（可选）
  - [ ] 8.1 创建查看会话信息的 API
    - GET /api/tasks/:taskId/neovate-session
    - 返回会话信息
    - _需求: 3.1_

  - [ ] 8.2 创建重置会话的 API
    - DELETE /api/tasks/:taskId/neovate-session
    - 删除会话映射
    - _需求: 3.2_

- [ ] 9. 添加定时清理任务
  - [ ] 9.1 实现定时清理调度器
    - 使用 node-cron 或类似库
    - 每小时执行一次过期会话清理
    - 记录清理结果
    - _需求: 5.4_

- [ ] 10. 集成测试和文档
  - [ ]* 10.1 编写集成测试
    - 测试完整的任务执行流程
    - 测试多轮对话场景
    - 测试会话恢复和失败处理

  - [ ] 10.2 更新文档
    - 更新 README.md 说明会话管理功能
    - 添加配置说明
    - 添加故障排除指南

  - [ ] 10.3 手动测试
    - 创建测试任务并执行多轮对话
    - 验证会话 ID 正确保存和恢复
    - 验证工作目录切换场景
    - 验证并发任务场景

- [ ] 11. 最终检查点
  - 确保所有测试通过，询问用户是否有问题
